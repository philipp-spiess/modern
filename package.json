{
  "name": "modern",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.3.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "tauri dev",
    "lint": "oxlint --fix --deny-warnings .",
    "format": "concurrently -n fmt,lint 'oxfmt .' 'bun run lint'",
    "check": "concurrently -n fmt,lint,ts 'oxfmt --check .' 'bun run lint' 'bun run --workspaces check'"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@typescript/native-preview": "7.0.0-dev.20251121.1",
    "concurrently": "^9.2.1",
    "oxfmt": "^0.14.0",
    "oxlint": "^1.29.0"
  },
  "dependencies": {
    "@tauri-apps/plugin-shell": "~2",
    "ai": "^5.0.98"
  },
  "patchedDependencies": {
    "@orpc/tanstack-query@1.11.3": "patches/@orpc%2Ftanstack-query@1.11.3.patch"
  }
}
